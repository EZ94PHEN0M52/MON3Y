# Get a free key at https://the-odds-api.com/
ODDS_API_KEY=your_key_here

# Optional — Sleeper Picks MLB props via Apify (https://console.apify.com/account/integrations)
# Required for sleeper_props.parquet on each fetch_data.py --props / run_daily.sh run.
APIFY_TOKEN=your_apify_token_here
